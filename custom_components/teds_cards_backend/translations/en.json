{
  "config": {
    "step": { "user": { "title": "Ted's Cards Backend", "description": "Add the alarms & timers backend for Ted's Cards." } },
    "abort": { "single_instance_allowed": "Already configured." }
  }
}
